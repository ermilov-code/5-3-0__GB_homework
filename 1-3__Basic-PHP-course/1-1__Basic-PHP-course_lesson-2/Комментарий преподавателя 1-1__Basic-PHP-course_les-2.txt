Очень много кода в комментах, не надо его комментить я же запускаю, делайте кадое задание в отдельном файле.

Множественный if elseif менее предпочтителен, лучше Switch. while верно, строго говоряна 2-ку два раза проверили, в идеале каждую проверку по разу, это можно было через While(true) цикл.

Закомменчено соответственно не работает.

Переменная переменных, это когда в переменной хранится не значение, а имя другой переменной.

${"taskName$i"} = readline . Создаете набор переменных на каждой итерации новые, а можно было сразу собирать через .= результат в строке для текста задачь, и считать сразу сумму в этом же цикле. Но архитектурно лучше было разделить ввод и формирование вывода.

$numberOfTasks тут надо было также как в 1 задании в цикле бесконечном спрашивать, пока число больше нуля не введут.
if (($numberOfTasks >= 1) && ($numberOfTasks <= 5)) ну вот не выполнилось условие и что? Надо было в цикле повторять запрос. Еще и основную логику вынесли в лишний блок излишне усложняя код.

Убирайте комменты чтобы код запускать можно было.